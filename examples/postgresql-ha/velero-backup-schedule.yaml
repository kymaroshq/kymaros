apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: postgres-ha-demo
  namespace: velero
spec:
  # Daily at 2:00 AM UTC
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - kymaros-demo-postgres
    defaultVolumesToFsBackup: true
    ttl: 168h0m0s  # Keep backups for 7 days
